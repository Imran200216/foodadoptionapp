import 'package:flutter/material.dart';

class AddFoodDonationScreen extends StatelessWidget {
  const AddFoodDonationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          "Add food donation scren",
        ),
      ),
    );
  }
}
